"""
======================================================================
Python 速查手册 — step1~10 核心提炼 + 扩展补遗
======================================================================
本文件汇总了 16 个 step 的精要 + 一张"你学会了什么"的全局图
======================================================================

=== 一、step1~10：你已掌握的 Python 基础 ===

┌─────────┬──────────────────────────────────┬──────────────────────┐
│ Step    │ 核心内容                         │ d2l 够用吗？         │
├─────────┼──────────────────────────────────┼──────────────────────┤
│ 1       │ 类 + __init__ + 方法 + with open │ ✅                   │
│ 2       │ 继承 / super() / 方法重写        │ ✅ 理解 nn.Module    │
│ 3       │ MRO 方法查找链                   │ ✅ 调试够用           │
│ 4       │ 魔术方法 (__len__/getitem/add..) │ ✅ 理解 Python 协议   │
│ 5       │ 对象交互（对象调对象）           │ ✅ 理解模块协作       │
│ 6       │ @classmethod / @staticmethod     │ ✅ 够用               │
│ 7       │ 属性查找链 / 可变对象陷阱       │ ⭐ 最重要，防坑      │
│ 8       │ 异常处理 try/except/raise        │ ✅ 必备技能           │
│ 9       │ 模块 import / pip / __name__     │ ✅ 能拆文件了         │
│ 10      │ @property getter/setter          │ ✅ 封装数据           │
└─────────┴──────────────────────────────────┴──────────────────────┘

  ╔══════════════════════════════════════════════════════╗
  ║  核心思维模型：Python 的一切都是"查找链"             ║
  ║                                                      ║
  ║    obj.attr → 实例.__dict__ → 类.__dict__ → 父类    ║
  ║    obj.attr = val → 看类里有没有 property 拦截       ║
  ║    @property 就是在类.__dict__ 里插了个钩子          ║
  ║    MRO 决定了找方法的顺序                            ║
  ╚══════════════════════════════════════════════════════╝

=== 二、扩展 step11~16：你刚补齐的 ===

┌─────────┬──────────────────────────────────┬──────────────────────┐
│ Step    │ 核心内容                         │ d2l 频率             │
├─────────┼──────────────────────────────────┼──────────────────────┤
│ 11      │ 列表推导式 [x**2 for x in ...]   │ 🔴 每页可见         │
│ 12      │ lambda x: x**2                   │ 🟡 常用             │
│ 13      │ zip / enumerate                  │ 🟡 数据处理必备     │
│ 14      │ 装饰器 @xxx                      │ 🟢 了解即可         │
│ 15      │ *args / **kwargs                 │ 🟢 装饰器里常见     │
│ 16      │ 生成器 yield / 生成器表达式       │ 🟢 DataLoader 本质  │
└─────────┴──────────────────────────────────┴──────────────────────┘

=== 三、快速自查表 ===

1. 类变量 vs 实例变量
   - 类变量：所有实例共享 → 存在 类.__dict__
   - 实例变量：各自独立   → 存在 实例.__dict__
   - 陷阱：成员列表千万别写在类里，放 __init__！

2. 读 vs 写
   - 读：实例 → 类 → 父类（顺着链找）
   - 写：永远在实例新建（除非类里有同名 property）

3. 异常处理三板斧
   - try: 可能出错的代码
   - except X: 捕获指定异常
   - raise ValueError("原因")：主动抛

4. 常用魔术方法
   - __len__ → len(obj)
   - __getitem__ → obj[i]
   - __str__ → print(obj)
   - __add__ → obj1 + obj2

5. @property 的三种形态
   - 只读 → 只有 @property（没有 setter）
   - 读写 → @property + @xxx.setter
   - 计算 → 每次读都重新算（面积/年龄）

6. 模块
   - import numpy as np
   - from torch import nn
   - if __name__ == '__main__':
   - pip install 包名

=== 四、扩展速记 ===

列表推导式:  [表达式 for 变量 in 数据 if 条件]
lambda:       lambda 参数: 返回值
zip:          for a, b in zip(列表1, 列表2)
enumerate:    for i, x in enumerate(列表)
装饰器:        @xxx = 给函数加功能
*args:        收位置参数为元组
**kwargs:     收关键字参数字典
生成器:        yield 暂停并返回值（省内存）

=== 五、接下来 → d2l ===

你现在具备的 Python 基础足够读 d2l 了。
遇到看不懂 Python 语法的地方，翻回这个文件查。

d2l 的主要挑战不是 Python，
而是：线性代数 / 概率 / 微积分的直觉 + PyTorch API。
"""
